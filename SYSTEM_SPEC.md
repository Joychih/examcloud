# 良師塾考試雲 - 系統規格書

> **版本**: v1.0.0  
> **更新日期**: 2026-01-19  
> **文件性質**: 前端 UI 規格 + 後端 API 需求規格

---

## 目錄

1. [專案概述](#1-專案概述)
2. [系統架構](#2-系統架構)
3. [使用者角色與權限](#3-使用者角色與權限)
4. [資料模型](#4-資料模型)
5. [頁面功能規格](#5-頁面功能規格)
6. [跨角色使用情境](#6-跨角色使用情境)
7. [API 端點需求](#7-api-端點需求)
8. [特色功能與賣點](#8-特色功能與賣點)

---

## 1. 專案概述

### 1.1 產品定位

「良師塾考試雲」是一套針對高中數學的線上練習系統，主打：

- **全台名校段考題庫**：收錄 9 區 45+ 所高中的段考真題
- **升學考試完整解析**：學測、分科測驗、國中會考 110-114 學年度歷屆試題
- **智慧學習追蹤**：知識點熟練度分析、弱點診斷
- **班級管理功能**：老師可指派作業、追蹤學生進度

### 1.2 技術架構

| 層級 | 技術選型 |
|------|----------|
| 前端框架 | React 18 + TypeScript |
| 建置工具 | Vite |
| 路由管理 | React Router DOM v6 |
| 數學渲染 | KaTeX |
| 樣式方案 | CSS Variables + 自訂元件 |
| 狀態管理 | React useState + Context (useAuth) |
| API 抽象 | 環境變數切換 Mock/Real API |

### 1.3 目錄結構

```
src/
├── App.tsx                 # 路由設定與全域佈局
├── main.tsx                # 應用程式入口
├── styles.css              # 全域樣式
├── components/             # 可重用元件
│   ├── AppShell.tsx        # 認證後頁面外殼（Header + Nav）
│   ├── PublicShell.tsx     # 公開頁面外殼
│   ├── RoleNav.tsx         # 角色導覽列
│   ├── QuestionBlock.tsx   # 題目渲染元件
│   ├── Latex.tsx           # LaTeX 數學公式渲染
│   ├── Modal.tsx           # 通用彈窗
│   ├── PaymentModal.tsx    # 付費升級彈窗
│   └── RequireRole.tsx     # 路由權限守衛
├── data/
│   ├── models.ts           # TypeScript 資料型別定義
│   ├── mock.ts             # Mock 資料與假資料庫
│   └── api.ts              # API 抽象層
├── hooks/
│   └── useAuth.ts          # 認證狀態 Hook
├── routes/                 # 頁面元件
│   ├── LoginPage.tsx
│   ├── StudentDashboard.tsx
│   ├── StudentExamList.tsx
│   ├── StudentExamPage.tsx
│   ├── StudentResultPage.tsx
│   ├── StudentRecords.tsx
│   ├── StudentTopicSearch.tsx
│   ├── StudentCustomExam.tsx
│   ├── CreatorDashboard.tsx
│   ├── CreatorQuestionEditor.tsx
│   ├── CreatorQuestionBank.tsx
│   ├── AdminDashboard.tsx
│   ├── AdminAnalytics.tsx
│   ├── AdminAnnouncements.tsx
│   └── AdminUsers.tsx
└── utils/
    ├── auth.ts             # 認證工具函式
    └── parseExamTitle.ts   # 試卷標題解析
```

---

## 2. 系統架構

### 2.1 前端路由架構

```
/login                          # 登入頁（公開）
│
├── /student                    # 學生路由群組
│   ├── /dashboard              # 首頁總覽
│   ├── /exams                  # 試題清單
│   ├── /exams/:examId          # 作答頁面
│   ├── /result/:resultId       # 答題結果頁
│   ├── /records                # 答題紀錄
│   ├── /topics                 # 主題搜題
│   └── /custom-exam/:examId    # 自訂試卷作答
│
├── /creator                    # 命題者路由群組
│   ├── /dashboard              # 命題者首頁
│   ├── /editor                 # 題目編輯器
│   └── /bank                   # 題庫管理
│
└── /admin                      # 管理者路由群組
    ├── /dashboard              # 管理者首頁
    ├── /analytics              # 數據分析
    ├── /announcements          # 公告管理
    └── /users                  # 使用者管理
```

### 2.2 認證與授權流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Login     │────▶│  useAuth    │────▶│  localStorage│
│   Page      │     │   Hook      │     │   Storage   │
└─────────────┘     └─────────────┘     └─────────────┘
                           │
                           ▼
                    ┌─────────────┐
                    │ RequireRole │
                    │  Component  │
                    └─────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌─────────┐  ┌─────────┐  ┌─────────┐
        │ Student │  │ Creator │  │  Admin  │
        │ Routes  │  │ Routes  │  │ Routes  │
        └─────────┘  └─────────┘  └─────────┘
```

### 2.3 API 抽象層設計

```typescript
// 環境變數控制
const useMock = import.meta.env.VITE_USE_MOCK === "true";

// API 函式範例
export const getExams = async (): Promise<Exam[]> => {
  if (useMock) return [...mockDb.exams];
  return apiFetch<Exam[]>("/api/exams");
};
```

---

## 3. 使用者角色與權限

### 3.1 角色定義

| 角色 | 代碼 | 說明 |
|------|------|------|
| 學生 | `student` | 練習題目、查看解析、追蹤學習進度 |
| 命題者 | `creator` | 建立題目、管理題庫、編輯試卷 |
| 管理者 | `admin` | 系統管理、數據分析、使用者管理、發布公告 |

### 3.2 學生帳號分級

| 類型 | className | 權限差異 |
|------|-----------|----------|
| 免費會員 | `"免費會員"` | 只能查看正確答案，無法查看詳解與影音 |
| 付費會員 | `"高一a班"` 等 | 完整功能：文字詳解、影音詳解、AI 解惑 |

**判斷邏輯**：
```typescript
const isVip = currentStudent.className !== "免費會員";
```

### 3.3 學生資料結構

```typescript
type StudentUser = {
  id: string;
  name: string;
  email?: string;
  phone?: string;
  school: string;          // 就讀學校（如：建國中學）
  className: string;       // 班級/會員類型（如：高一a班、免費會員）
  grade: string;           // 年級（高一、高二、高三）
  region: string;          // 地區（基北區、桃連區...）
  plan: "free" | "vip";    // 方案（由 className 決定）
  joinDate: string;
  lastActiveDate?: string;
  examsTaken?: number;
  avgScore?: number;
  assignedExams?: string[];
  assignments?: string[];
};
```

---

## 4. 資料模型

### 4.1 學校 (School)

```typescript
type School = {
  id: string;
  name: string;           // 如：建國中學
  region: string;         // 如：基北區
  examCount: number;      // 該校試卷數量
  isFreeTrial: boolean;   // 是否開放免費試用
};
```

**涵蓋學校（9 區 45+ 所）**：
- 基北區：建國中學、北一女中、師大附中、成功高中、中山女中...
- 桃連區：武陵高中、中壢高中、桃園高中...
- 竹苗區：新竹實驗中學、新竹高中、新竹女中...
- 中投區：台中一中、台中女中、興大附中...
- 彰化區：彰化高中、彰化女中、精誠中學...
- 雲林區：斗六高中、虎尾高中...
- 嘉義區：嘉義高中、嘉義女中...
- 台南區：台南一中、台南女中...
- 高雄區：高雄中學、高雄女中...

### 4.2 題目 (Question)

```typescript
type QuestionType = "MCQ" | "TF" | "Fill" | "Calc" | "Proof" | "Written";

type Question = {
  id: string;
  type: QuestionType;
  content: string;              // 題目內容（支援 LaTeX）
  options?: string[];           // 選項（MCQ 專用）
  correctAnswer: string;        // 正確答案
  textExplanation: string;      // 文字詳解
  videoUrl?: string;            // 影音詳解連結
  images?: string[];            // 題目圖片
  tags: string[];               // 知識點標籤（tags[0] = 章節）
  difficulty: "easy" | "medium" | "hard";
  
  // 應用題專用
  maxScore?: number;
  gradingCriteria?: string;
  allowImageUpload?: boolean;
  
  // 來源標籤
  source?: {
    examCategory: ExamCategory;
    schoolId?: string;
    schoolName?: string;
    year?: string;
    grade?: string;
    subject?: string;
    examTitle?: string;
  };
};
```

**題型說明**：

| 代碼 | 中文 | 說明 |
|------|------|------|
| MCQ | 選擇題 | 單選，自動批改 |
| TF | 是非題 | 是/否，自動批改 |
| Fill | 填充題 | 文字輸入，自動批改 |
| Calc | 計算題 | 需顯示計算過程 |
| Proof | 證明題 | 手寫上傳，AI 輔助批改 |
| Written | 手寫應用題 | 手寫上傳，AI 輔助批改 |

### 4.3 試卷 (Exam)

```typescript
type ExamCategory = "school" | "junior_high" | "gsat" | "ast";

type Exam = {
  id: string;
  examCategory: ExamCategory;
  schoolId?: string;
  grade: string;              // 高一、高二、高三
  subject: string;            // 數學、數A、數B、數甲、數乙
  year: string;               // 110、111、112、113、114
  semester?: string;          // 上、下
  examNo?: string;            // 1、2、3（第幾次段考）
  title: string;              // 完整標題
  isPremium: boolean;         // 是否為付費內容
  questions: Question[];
  createdAt?: string;         // 建立時間（用於排序）
};
```

**考試類別說明**：

| 代碼 | 中文 | 篩選條件 |
|------|------|----------|
| school | 學校段考 | 學校、年級、年度、學期、第幾次 |
| junior_high | 國中會考 | 年度、科目 |
| gsat | 高中學測 | 年度、科目 |
| ast | 高中分科測驗 | 年度、科目 |

### 4.4 作答紀錄 (ExamResult)

```typescript
type AnswerRecord = {
  questionId: string;
  answer: string;
  isCorrect: boolean;
  score?: number;           // 實得分數
  maxScore?: number;        // 滿分
  imageUrl?: string;        // 手寫答案圖片
  aiGrading?: {
    score: number;
    feedback: string;
    confidence: number;
    gradedAt: string;
  };
};

type ExamResult = {
  id?: string;
  examId: string;
  schoolId?: string;
  score: number;
  total: number;
  answers: AnswerRecord[];
  submittedAt: string;
  userId?: string;
  assignmentId?: string;    // 來自指派的作業 ID
};
```

### 4.5 自訂試卷 (CustomExam)

```typescript
type CustomExam = {
  id: string;
  title: string;
  chapter: string;
  difficulty: "easy" | "medium" | "hard" | "mixed";
  questionIds: string[];
  createdAt: string;
};
```

### 4.6 試卷指派 (ExamAssignment)

```typescript
type ExamAssignment = {
  id: string;
  name: string;               // 指派名稱
  examId: string;             // 試卷 ID（可為 custom:xxx）
  examTitle: string;
  targetStudentIds: string[];
  createdAt: string;
  dueDate?: string;
};
```

### 4.7 公告 (Announcement)

```typescript
type Announcement = {
  id: string;
  title: string;
  content: string;
  type: "info" | "new" | "promo" | "important";
  targetGrades: string[];     // 空 = 全部
  targetClasses: string[];
  targetRegions: string[];
  createdAt: string;
  expiresAt?: string;
};
```

---

## 5. 頁面功能規格

### 5.1 登入頁 (LoginPage)

**路徑**: `/login`

**功能**：
1. 角色選擇
   - 學生、命題者、管理者三種角色按鈕
   - 點擊後設定 localStorage 並跳轉對應首頁

2. 學生登入流程
   - 顯示所有可用學生帳號列表
   - 班級篩選（免費會員、高一a班、高一b班...）
   - 快速登入按鈕
   - 卡片顯示：姓名、學校、年級、地區、會員類型

**UI 元素**：
- 角色選擇按鈕組
- 班級篩選標籤
- 學生卡片網格

---

### 5.2 學生首頁 (StudentDashboard)

**路徑**: `/student/dashboard`

**功能**：

1. **Hero Banner**
   - 歡迎訊息
   - 快速統計（10,000+ 題目、5年歷屆、AI 解惑）
   - 「開始練習」與「主題搜題」快捷按鈕
   - 動態粒子背景動畫

2. **指派試卷區**（條件顯示）
   - 黃色高亮區塊
   - 顯示老師指派的未完成作業
   - 每項顯示：指派名稱、試卷名稱、題數
   - 「開始作答」按鈕
   - 已完成的自動隱藏

3. **公告區**
   - 根據學生年級/班級/地區篩選顯示
   - 公告類型圖示與顏色區分
   - 最多顯示 3 則

4. **數據卡片**
   - 練習次數
   - 平均分數
   - 待加強知識點

5. **VIP 推廣區**（免費會員顯示）
   - 升級優勢說明
   - 「立即升級」按鈕

6. **快速入口卡片**
   - 開始練習
   - 主題搜題
   - 答題紀錄
   - 學習分析

---

### 5.3 試題清單 (StudentExamList)

**路徑**: `/student/exams`

**功能**：

1. **多層篩選器**
   - 考試種類：學校段考 / 國中會考 / 高中學測 / 高中分科測驗
   - 學校段考篩選：學校、年級、科目、年度、學期、第幾次
   - 升學考試篩選：年度、科目

2. **試卷列表**
   - 卡片式顯示
   - 顯示：標題、題數、考試類型標籤
   - 新試卷標記（7 天內建立顯示 🆕）
   - 付費內容鎖定圖示

3. **排序邏輯**
   - 預設按建立時間降序（最新在前）

4. **付費機制**
   - 免費會員點擊付費試卷時彈出升級視窗

---

### 5.4 作答頁面 (StudentExamPage)

**路徑**: `/student/exams/:examId`

**功能**：

1. **試卷標題區**
   - 試卷名稱
   - 題數統計
   - 已作答/總題數進度

2. **題目區**
   - 使用 `QuestionBlock` 元件渲染
   - 支援 LaTeX 數學公式
   - 選擇題：Radio Button
   - 是非題：是/否按鈕
   - 填充題：文字輸入框
   - 題目圖片顯示

3. **作答狀態**
   - 即時顯示已作答題數
   - 提交按鈕

4. **提交後顯示**
   - 分數統計（答對/總題數）
   - 每題顯示正確/錯誤狀態（顏色標記）
   - 四個解析區塊：
     - 正確答案（全員可見）
     - 文字詳解（VIP 專屬）
     - 影音詳解（VIP 專屬）
     - AI 解惑（預留區塊）

---

### 5.5 答題結果頁 (StudentResultPage)

**路徑**: `/student/result/:resultId`

**功能**：
- 從答題紀錄點擊「查看詳情」進入
- 顯示歷史作答的完整內容
- 與作答頁面提交後相同的解析介面
- 題目為唯讀狀態（無法修改答案）

---

### 5.6 答題紀錄 (StudentRecords)

**路徑**: `/student/records`

**功能**：

1. **紀錄列表**
   - 卡片式顯示每次作答
   - 顯示：類型標籤、指派標籤、試卷名稱、時間、分數
   - 指派作業以黃色邊框標示

2. **類型標籤**
   - 段考（藍色）
   - 會考（綠色）
   - 學測（黃色）
   - 分科（紅色）
   - 主題練習（紫色）
   - 📋 指派（橙色）

3. **段考題全體比較**
   - 展開顯示：
     - 我的分數 vs 全體平均
     - 五標位置（頂標、前標、均標、後標、底標）
     - 分數分布直方圖
     - 每題答對率比較

4. **查看詳情按鈕**
   - 跳轉至 `StudentResultPage`

---

### 5.7 主題搜題 (StudentTopicSearch)

**路徑**: `/student/topics`

**功能**：

1. **層級選擇**
   - 高中數學 / 國中數學

2. **年級篩選**（高中限定）
   - 高一 / 高二 / 高三
   - 每個年級顯示對應章節

3. **知識點選擇器**
   - 分類群組顯示（代數基礎、數列機率、三角向量...）
   - 每個知識點顯示：
     - 題庫數量
     - 熟練度百分比（顏色區分）
     - 答題次數
   - 支援多選

4. **熟練度顏色**
   - 🟢 精熟（80%+）
   - 🟢 良好（60-79%）
   - 🟡 普通（40-59%）
   - 🔴 待加強（<40%）
   - ⚪ 未作答

5. **難度篩選**
   - 基礎 / 中階 / 進階（支援多選）

6. **生成試卷功能**
   - 選擇難度：基礎/中階/進階/混合
   - 選擇題數：5/10/15/20/25/30
   - 點擊「開始練習」生成自訂試卷

7. **題庫瀏覽**
   - 簡潔/詳細切換
   - 顯示題目預覽、標籤、難度

---

### 5.8 自訂試卷作答 (StudentCustomExam)

**路徑**: `/student/custom-exam/:examId`

**功能**：
- 與一般作答頁面相同
- 紫色 Header 區分
- 提交後記錄到答題紀錄
- examId 格式：`custom:xxx`

---

### 5.9 命題者首頁 (CreatorDashboard)

**路徑**: `/creator/dashboard`

**功能**：
- 快速入口：建立題目、管理題庫
- 統計數據（待實作）

---

### 5.10 題目編輯器 (CreatorQuestionEditor)

**路徑**: `/creator/editor`

**功能**：

1. **兩階段流程**
   - 第一步：填寫試卷基本資料
   - 第二步：新增題目

2. **試卷資訊**
   - 考試類別：學校段考 / 學測 / 會考 / 分科測驗
   - 學校段考：學校（下拉/手動輸入）、年級、科目、年度、學期、段考次數
   - 升學考試：年度、科目
   - 自動生成標題

3. **選擇現有試卷**
   - 搜尋並載入現有試卷繼續編輯

4. **題目編輯**
   - 題型選擇：選擇題/是非題/填充題
   - 題目內容（支援 LaTeX）
   - 選項編輯（選擇題）
   - 正確答案
   - 章節標籤
   - 難度選擇
   - 文字詳解
   - 影音詳解（支援多個）

5. **快速填入**
   - 測試用預設內容

6. **批次操作**
   - 新增多題
   - 一次儲存全部

---

### 5.11 題庫管理 (CreatorQuestionBank)

**路徑**: `/creator/bank`

**功能**：

1. **篩選器**
   - 題型
   - 難度
   - 章節標籤
   - 關鍵字搜尋
   - 來源篩選：考試類別、學校、年度、年級、科目

2. **題目列表**
   - 顯示：題目內容、標籤、難度、來源
   - 編輯按鈕
   - 刪除按鈕

3. **編輯功能**
   - 彈窗編輯題目
   - 修改所有欄位
   - 新增多個影音詳解

---

### 5.12 管理者首頁 (AdminDashboard)

**路徑**: `/admin/dashboard`

**功能**：
- 系統概覽（待實作）
- 快速入口

---

### 5.13 數據分析 (AdminAnalytics)

**路徑**: `/admin/analytics`

**功能**：

1. **統計卡片**
   - 總作答次數
   - 總使用者數
   - 平均分數

2. **最新作答列表**
   - 顯示：學生姓名、班級、試卷名稱、分數、時間
   - 班級篩選
   - 分數顏色標記
   - 「查看詳情」按鈕

3. **詳情彈窗**
   - 顯示該次作答的完整解析介面

---

### 5.14 公告管理 (AdminAnnouncements)

**路徑**: `/admin/announcements`

**功能**：

1. **發布公告表單**
   - 公告類型：一般通知/新功能/優惠促銷/重要公告
   - 標題（必填）
   - 內容（必填）
   - 目標對象篩選：
     - 年級（多選）
     - 班級（多選）
     - 地區（多選）
   - 預估發送人數即時計算

2. **公告列表**
   - 類型圖示與顏色
   - 標題、內容
   - 發布時間
   - 目標對象資訊
   - 刪除按鈕

---

### 5.15 使用者管理 (AdminUsers)

**路徑**: `/admin/users`

**功能**：

1. **分頁切換**
   - 👥 學生管理
   - 📋 指派紀錄

2. **統計卡片**
   - 總學生數
   - VIP 會員數
   - 指派紀錄數
   - 已選取人數

3. **篩選器**
   - 班級
   - 年級
   - 地區
   - 方案（免費/VIP）
   - 關鍵字搜尋

4. **學生列表**
   - 勾選框（支援全選）
   - 顯示：姓名、Email、班級、年級、學校、地區、方案、加入日期、完成試卷數、平均分、指派數

5. **批次指派功能**
   - 選擇學生後點擊「📋 指派試卷」
   - 三種指派模式：
     - 📝 選擇試卷：搜尋現有段考
     - 🎯 主題生成：選擇章節、難度、題數
     - ✅ 勾選題目：自選題目組卷
   - 指派名稱設定（預設：試卷名稱 + 班級 + 日期）

6. **指派紀錄分頁**
   - 每項顯示：
     - 指派名稱
     - 試卷名稱
     - 完成率（百分比 + 進度條）
     - 學生完成狀態（✓ 已完成 / ○ 未完成）

---

## 6. 跨角色使用情境

### 6.1 情境一：老師指派段考複習給學生

**角色流程**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 管理者（老師）                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 1. 登入系統，選擇「管理者」角色                                    │
│ 2. 進入「使用者管理」頁面                                         │
│ 3. 篩選「高一a班」學生                                           │
│ 4. 全選該班學生                                                  │
│ 5. 點擊「📋 指派試卷」                                           │
│ 6. 選擇「📝 選擇試卷」模式                                        │
│ 7. 搜尋「建國中學 高一 第一次段考」                                │
│ 8. 設定指派名稱：「建國中學 114 高一第一次段考 - 高一a班 (1/19)」   │
│ 9. 確認指派                                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 學生                                                             │
├─────────────────────────────────────────────────────────────────┤
│ 1. 登入系統，選擇「高一a班」任一學生                               │
│ 2. 首頁看到黃色「老師指派的練習」區塊                              │
│ 3. 點擊「開始作答」                                              │
│ 4. 完成作答並提交                                                │
│ 5. 查看解析與詳解                                                │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 管理者（老師）                                                    │
├─────────────────────────────────────────────────────────────────┤
│ 1. 進入「使用者管理」→「📋 指派紀錄」                              │
│ 2. 查看該指派的完成率                                            │
│ 3. 確認哪些學生已完成、哪些未完成                                  │
│ 4. 必要時透過公告提醒未完成學生                                    │
└─────────────────────────────────────────────────────────────────┘
```

### 6.2 情境二：命題者新增段考題目

**角色流程**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 命題者                                                           │
├─────────────────────────────────────────────────────────────────┤
│ 1. 登入系統，選擇「命題者」角色                                    │
│ 2. 進入「建立題目」頁面                                          │
│ 3. 填寫試卷資訊：                                                │
│    - 考試類別：學校段考                                          │
│    - 學校：建國中學                                              │
│    - 年級：高一                                                  │
│    - 科目：數學                                                  │
│    - 年度：114                                                   │
│    - 學期：上                                                    │
│    - 段考次數：1                                                 │
│ 4. 系統自動生成標題                                              │
│ 5. 新增題目（可多題）                                            │
│ 6. 儲存試卷                                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 學生                                                             │
├─────────────────────────────────────────────────────────────────┤
│ 1. 進入「試題清單」                                              │
│ 2. 看到新試卷（顯示 🆕 新增 標籤）                                │
│ 3. 點擊開始練習                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 情境三：學生自主複習弱點

**角色流程**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 學生                                                             │
├─────────────────────────────────────────────────────────────────┤
│ 1. 進入「答題紀錄」查看歷史成績                                    │
│ 2. 發現「三角函數」答對率偏低                                     │
│ 3. 進入「主題搜題」                                              │
│ 4. 選擇「高一」→「三角函數」章節                                  │
│ 5. 查看熟練度：顯示「42% 待加強」                                 │
│ 6. 設定生成試卷：                                                │
│    - 難度：基礎                                                  │
│    - 題數：10 題                                                 │
│ 7. 點擊「開始練習」                                              │
│ 8. 完成練習並查看詳解                                            │
│ 9. 回到主題搜題，熟練度提升                                       │
└─────────────────────────────────────────────────────────────────┘
```

### 6.4 情境四：管理者發布公告

**角色流程**：

```
┌─────────────────────────────────────────────────────────────────┐
│ 管理者                                                           │
├─────────────────────────────────────────────────────────────────┤
│ 1. 進入「公告管理」頁面                                          │
│ 2. 點擊「+ 發布新公告」                                          │
│ 3. 填寫公告：                                                    │
│    - 類型：新功能/題目                                           │
│    - 標題：🎉 新題目上線！113學年度學測完整解析                    │
│    - 內容：詳細說明...                                           │
│    - 目標年級：高三（僅高三看到）                                  │
│ 4. 查看預估發送人數                                              │
│ 5. 發布公告                                                      │
└─────────────────────────────────────────────────────────────────┘
                              │
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│ 高三學生                                                         │
├─────────────────────────────────────────────────────────────────┤
│ 1. 登入系統                                                      │
│ 2. 首頁公告區看到此公告                                          │
│ 3. 點擊前往試題清單                                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 7. API 端點需求

### 7.1 認證 API

| 方法 | 端點 | 說明 |
|------|------|------|
| POST | `/api/auth/login` | 登入（回傳 token + user info） |
| POST | `/api/auth/logout` | 登出 |
| GET | `/api/auth/me` | 取得當前使用者資訊 |

### 7.2 學校 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/schools` | 取得所有學校列表 |
| GET | `/api/schools/:id` | 取得單一學校資訊 |

### 7.3 試卷 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/exams` | 取得試卷列表（支援篩選） |
| GET | `/api/exams/:id` | 取得單一試卷（含題目） |
| POST | `/api/exams` | 建立新試卷 |
| PUT | `/api/exams/:id` | 更新試卷 |
| DELETE | `/api/exams/:id` | 刪除試卷 |

**篩選參數**：
- `category`: school | junior_high | gsat | ast
- `schoolId`: 學校 ID
- `grade`: 年級
- `subject`: 科目
- `year`: 年度
- `semester`: 學期
- `examNo`: 段考次數

### 7.4 題目 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/questions` | 取得題目列表（題庫） |
| GET | `/api/questions/:id` | 取得單一題目 |
| POST | `/api/questions` | 建立新題目 |
| PUT | `/api/questions/:id` | 更新題目 |
| DELETE | `/api/questions/:id` | 刪除題目 |
| POST | `/api/questions/filter` | 篩選題目（用於生成試卷） |

### 7.5 作答紀錄 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/results` | 取得作答紀錄列表 |
| GET | `/api/results/:id` | 取得單一作答紀錄 |
| POST | `/api/results` | 提交作答結果 |

### 7.6 自訂試卷 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/custom-exams/:id` | 取得自訂試卷 |
| POST | `/api/custom-exams` | 建立自訂試卷 |

### 7.7 學生 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/students` | 取得學生列表 |
| GET | `/api/students/:id` | 取得單一學生 |
| PUT | `/api/students/:id` | 更新學生資料 |

### 7.8 指派 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/assignments` | 取得指派列表 |
| GET | `/api/assignments/:id` | 取得單一指派 |
| POST | `/api/assignments` | 建立指派 |
| DELETE | `/api/assignments/:id` | 刪除指派 |

### 7.9 公告 API

| 方法 | 端點 | 說明 |
|------|------|------|
| GET | `/api/announcements` | 取得公告列表 |
| POST | `/api/announcements` | 建立公告 |
| DELETE | `/api/announcements/:id` | 刪除公告 |

---

## 8. 特色功能與賣點

### 8.1 核心競爭優勢

| 特色 | 說明 | 對標競品差異 |
|------|------|--------------|
| 🏫 全台名校題庫 | 9 區 45+ 所高中段考真題 | 非自編題，來源可信 |
| 📊 熟練度追蹤 | 每個知識點顯示答對率與次數 | 精準定位弱點 |
| 👨‍🏫 班級管理 | 老師可指派作業、追蹤進度 | 適合補習班使用 |
| 🎯 智慧組卷 | 依章節、難度自動生成試卷 | 個人化練習 |
| 📱 響應式設計 | 手機、平板、電腦皆可使用 | 隨時隨地練習 |

### 8.2 學生端賣點

1. **全方位題庫**
   - 段考：45+ 所名校真題
   - 學測：110-114 學年度完整解析
   - 會考：國中升學必備
   - 分科：指考轉型後最新題型

2. **學習追蹤**
   - 答題紀錄完整保存
   - 知識點熟練度視覺化
   - 與全體比較（段考）

3. **VIP 專屬**
   - 完整文字詳解
   - 影音詳解
   - AI 解惑（規劃中）

### 8.3 教師端賣點

1. **班級管理**
   - 一鍵指派作業給整班
   - 即時查看完成進度
   - 識別未完成學生

2. **靈活指派**
   - 選擇現有試卷
   - 依主題自動組卷
   - 手動挑選題目

3. **數據分析**
   - 學生作答統計
   - 班級整體表現

### 8.4 管理端賣點

1. **公告系統**
   - 精準推播（依年級/班級/地區）
   - 多種公告類型

2. **使用者管理**
   - 完整學生資料
   - 多維度篩選
   - 批次操作

### 8.5 技術亮點

1. **LaTeX 數學渲染**
   - 完美顯示數學公式
   - 支援複雜符號與排版

2. **API 抽象層**
   - Mock/Real 一鍵切換
   - 後端開發零阻礙

3. **TypeScript 全覆蓋**
   - 型別安全
   - 自動文件

4. **響應式設計**
   - Mobile First
   - 適配各種裝置

---

## 附錄 A：Mock 資料說明

### A.1 學生帳號

| ID | 姓名 | 班級 | 年級 | 地區 |
|----|------|------|------|------|
| student-f01~f05 | 免費會員 | 免費會員 | 混合 | 混合 |
| student-001~005 | 高一a班 | 高一a班 | 高一 | 基北區 |
| student-006~008 | 高一b班 | 高一b班 | 高一 | 桃連區 |
| student-009~011 | 高二a班 | 高二a班 | 高二 | 基北區 |
| student-012~013 | 高二b班 | 高二b班 | 高二 | 桃連區 |
| student-014~017 | 高三a班 | 高三a班 | 高三 | 中投區 |

### A.2 試卷生成規則

- 學校段考：5 年 × 3 年級 × 2 學期 × 3 次 × 45 學校 = 4,050 份
- 學測：5 年 × 2 科（數A、數B）= 10 份
- 會考：5 年 × 1 科 = 5 份
- 分科：5 年 × 2 科（數甲、數乙）= 10 份

### A.3 知識點章節

**高一**：數與式、多項式、方程式、不等式、數列與級數、排列組合、三角函數、指數與對數

**高二**：向量、平面向量、空間向量、圓與球、圓錐曲線、直線方程、機率、統計、矩陣、複數

**高三**：極限、微分、積分

---

## 附錄 B：環境變數

| 變數名 | 說明 | 預設值 |
|--------|------|--------|
| `VITE_USE_MOCK` | 是否使用 Mock 資料 | `"true"` |
| `VITE_API_BASE_URL` | API 基礎路徑 | `/api` |

---

## 附錄 C：開發指令

```bash
# 安裝依賴
npm install

# 開發模式
npm run dev

# 建置
npm run build

# 預覽建置結果
npm run preview
```

---

**文件結束**

> 本文件由前端團隊編撰，如有疑問請聯繫專案負責人。
